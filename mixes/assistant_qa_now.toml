#:schema ../datamix/mixture.schema.json
# assistant_qa assembled from the rewrites that are DONE, without waiting for
# the rest. Identical to `assistant_qa.toml` except for three things:
#
#   * [llm] cache_only = true — no socket is opened. A record whose Python->Rust
#     rewrite has not come back yet is dropped rather than generated, so this
#     build takes minutes and holds no untranslated Python. Re-run it whenever
#     the long build has made progress and the corpus grows.
#   * a different output path, so the long build's eventual corpus and this one
#     do not overwrite each other.
#   * smoltalk_long weighted higher still (45), because answer quality decays
#     with conversation depth and this is the only source with more than one
#     exchange in it.
#
#   cargo run -p datamix --release -- build mixes/assistant_qa_now.toml

[output]
kind = "sft"
path = "data/mix/assistant_qa_now.jsonl"
report = "data/mix/assistant_qa_now_report.md"
seed = 1234
holdout = 0.02
# A weight is a share of the *examples*, not of the tokens: a tool exchange is
# a few hundred bytes and a magpie answer is thousands, so token weights put
# the model's register in the hands of whichever source is wordiest.
#
# Every source is `epochs = 1`: no example is ever trained on twice. Data is not
# the constraint here, so a source that cannot fill its share gives what it has
# and the corpus is smaller — repeating it would be paying in overfitting for a
# number that only looks bigger.
weight_by = "records"
records = 0

# The rewrite runs on a local model, so the build needs the server up:
#   lms ps                     # the loaded model's id and its PARALLEL count
#   cargo run -p datamix --release -- ping mixes/assistant_qa.toml
[llm]
endpoint = "http://localhost:1234/v1"
model = "unsloth/qwen3.8-27b"
max_tokens = 4096          # the longest Python record here is 10.8 kB, ~3.1k tokens out
timeout = 900
workers = 8                # LM Studio's PARALLEL (lms load --parallel 8); 12 is slower
cache_only = true          # assemble from what is already translated; never call out
completion_stop = "<|im_end|>"
# Driving /v1/completions with the template written out is the only way to
# prefill an empty <think>, and prefilling it is the only thing that turns this
# model's deliberation off — chat_template_kwargs, /no_think and
# reasoning.enabled are all ignored by LM Studio. Measured on this corpus:
# 4.3 s against 43.6 s per record, and no reply truncated mid-answer.
completion_template = """<|im_start|>system
{system}<|im_end|>
<|im_start|>user
{user}<|im_end|>
<|im_start|>assistant
<think>

</think>

"""

[filter]
max_words = 4096
max_tokens = 28672
min_bytes = 16
dedup = "exact"

# Python out, Rust in. The model is meant to write Rust — the pretraining corpus
# is StarCoder-Rust and rust-lib — while SmolTalk's code half is overwhelmingly
# Python: 36,975 ```python fences against 104 ```rust ones, in 24% of the
# records. Left alone that is what the model learns code *is*.
#
# The rewrite is cached on the record's own text, so it survives a rebuild: the
# first build pays for it once and every later build reads the answers back and
# makes no call at all.
#
# `data/llm-cache` is the only place these translations exist. It sits under
# data/ rather than target/ on purpose: it is ~56 h of local generation, and a
# `cargo clean` used to take it with it. Deleting it redoes the whole rewrite.
# `mixes/rustified.toml` materializes it into a plain JSONL that any mixture
# can read without the server.
#
# It sits in [filter] rather than on one source because Python turns up in more
# than the code subsets.
#
# The gate is deliberately wide. The word alone is enough — an answer that recommends
# Python without showing any is the same defect — and so are the ecosystem
# names that give it away when the word itself never appears. A record matching
# none of these never reaches the server; one that matches but turns out to be
# about a snake comes back unchanged, which the prompt below asks for
# explicitly.
transform_when = [
    "```python", "```py", "python", "Python",
    "pip install", "numpy", "pandas", "matplotlib", "scikit", "PyTorch",
    "Django", "Flask", "Jupyter", "virtualenv", "__init__",
]
transform_temperature = 0.2
transform = """
You convert Python programming data into Rust. You are given one conversation as {"messages": [...]}.

Rewrite it so that no trace of Python is left and the exchange is about Rust instead. That means all of it: the user's request, any code inside the question, the assistant's prose, every code block, and any mention of Python libraries, tools or idioms by name. Where a Python library is named, use the Rust crate a Rust programmer would actually reach for.

Hold everything else still. The same number of turns, in the same order, with the same roles. The same task, the same difficulty, the same level of detail, roughly the same length. If the original explains its reasoning step by step, so does yours.

Write Rust the way it is actually written: ```rust fences, Result and ? for anything fallible rather than exceptions, iterators where a loop would be noise, &str and String used correctly, assert_eq! for the assertions. The code must be code that compiles.

This is not only about code. An answer that merely *recommends* Python — "you could use Python for this", "the pandas library handles that", a `pip install` line — is the same defect as a Python code block, and gets the same treatment: recommend Rust and the crate a Rust programmer would actually reach for.

Do not mention Python anywhere in the result, not even in passing and not to compare the two languages. The reader must have no way to tell the exchange was ever about anything else.

The word is not always the language. A carpet python, a Colt Python, Monty Python and a #Python hashtag inside a quoted document are not programming and must be left exactly as they are — rewriting those would corrupt the text. If the conversation happens to contain no Python the language, return it unchanged.

Reply with ONLY the rewritten {"messages": [...]} JSON object. No commentary, no code fence around it.
"""

# The corpus's system turns are 246 distinct strings, twelve of which cover
# 32% of every record — "You're an AI assistant for text re-writing." alone
# opens 13.6% of them. A constant that large is a prop: the model learns to
# answer with it in front of the prompt, and at inference it is never there.
#
# Only the identity sentence goes. What follows it is usually the record's ONLY
# instruction ("Rewrite the input text to make it more professional", with the
# user turn holding just the raw email), so cutting the whole turn would leave
# 23k records that cannot be answered from what is left. Where the framing is
# all there was, the turn disappears on its own.
#
# `system_fold = true` would go further and move the surviving instruction into
# the user turn, which is where inference puts it. Left off: it changes more
# than was asked for.
system_strip = [
    "You are an AI rewriting assistant. You will be provided with a text and you need to rewrite it according to the user's instructions.",
    "You are an AI assistant that helps people find information.",
    "You are an AI assistant that follows instruction extremely well.",
    "You are a helpful assistant, who always provide explanation.",
    "You're an AI assistant for text re-writing.",
    "You are an AI assistant.",
]
system_fold = false

# Every ```rust block the rewrite produced goes through rustc, and a record
# whose code does not build is dropped. A model translating Python into Rust
# makes the kind of mistake that reads fine and does not compile — a string
# literal eaten by a substitution (`Node { name: ".to_string()" }`), a crate
# invented to stand in for a Python library. rustc settles that in ~0.3 s
# against the ~4 s the rewrite itself cost.
#
# Measured on 24 rewritten records: 63% built as they came back, and one repair
# pass carrying rustc's own errors took that to 79%. A second pass fixed
# nothing, which is why `verified` only ever makes one.
verify = "rust"
verify_repair = """
You are fixing Rust code inside one training conversation. You are given the conversation as {"messages": [...]} and the exact rustc errors its code produces.

Fix only what the errors point at. Keep the same turns, the same roles, the same prose and the same task. Do not switch language, do not add dependencies to work around an error, and do not delete the code to make the error go away. If the prose describes what the code does, keep the two in agreement.

Reply with ONLY the corrected {"messages": [...]} JSON object. No commentary.
"""

# Who it is and how it sounds when nothing needs doing. Without this, asked
# "who are you?", the model answers from whatever persona data dominates.
[source.persona]
kind = "synth"
path = "mixes/synth/persona.syn"
count = 1500
epochs = 1
weight = 1

[source.lamp]
kind = "synth"
path = "mixes/synth/lamp.syn"
count = 3000
epochs = 1
weight = 0.35

[source.apps]
kind = "synth"
path = "mixes/synth/apps.syn"
count = 3000
epochs = 1
weight = 0.35

# Tool calls that are not the first turn of an empty conversation: after small
# talk, under a system context, with the conversation carrying on afterwards.
[source.tools_in_context]
kind = "synth"
path = "mixes/synth/tools_in_context.syn"
count = 4000
epochs = 1
weight = 0.3

# Short, natural chat — the register everything else is measured against.
# `explore-instruct-rewrite` only: 3017 conversations, 3009 distinct assistant
# turns, most-repeated string twice. Short AND varied.
[source.smoltalk_short]
kind = "chat"
path = "/home/unschlagbar/training_data/smol-smoltalk"
select_column = "source"
select_values = ["explore-instruct-rewrite"]
trim_turns = true
epochs = 1
weight = 3

# `everyday-conversations`, kept small on purpose. 2241 of its 8595 assistant
# turns are the single string "Hello! How can I help you today?" — 8x more
# repeated than anything else in SmolTalk. `epochs = 1` does not protect against
# that: no RECORD repeats, but thousands of them carry the same reply, and a
# model trained on it answers every short prompt with a greeting. It still has
# the multi-turn everyday register nothing else provides, so it stays — at a
# weight that makes it a seasoning rather than the dish.
[source.smoltalk_chitchat]
kind = "chat"
path = "/home/unschlagbar/training_data/smol-smoltalk"
select_column = "source"
select_values = ["everyday-conversations"]
trim_turns = true
epochs = 1
# The only source in the corpus that goes past three exchanges: 80% of it is
# four. Raised from 0.5 because answer quality decays with conversation depth
# and 86.6% of the corpus was a single exchange — see smoltalk_long below.
weight = 2

# Rewriting, summarizing, constrained answers: real tasks, moderate length.
[source.smoltalk_tasks]
kind = "chat"
path = "/home/unschlagbar/training_data/smol-smoltalk"
select_column = "source"
select_values = ["smol-summarize-20k", "smol-contraints", "smollm-rewrite-30k"]
trim_turns = true
epochs = 1
weight = 25

# General knowledge and code, so trimming the essays does not cost it substance.
[source.smoltalk_knowledge]
kind = "chat"
path = "/home/unschlagbar/training_data/smol-smoltalk"
select_column = "source"
select_values = ["openhermes-50k", "self-oss-instruct"]
trim_turns = true
epochs = 1
weight = 25

# Despite the name, this is the corpus's multi-turn source, not just its
# long-form one: 98.3% of it is exactly three exchanges, where every other
# SmolTalk subset here is a single one. Raised from 12 because the model's
# answers degrade as a conversation gets longer, and 86.6% of the corpus was
# teaching it only how to answer turn one.
#
# It cannot fix depth past three — nothing in this corpus reaches turn four, and
# all ten of this SmolTalk copy's subsets are already in use, so deeper
# conversations have to be generated rather than sourced. Raising the weight
# costs no extra rewriting: the source reads the same 279k records either way.
[source.smoltalk_long]
kind = "chat"
path = "/home/unschlagbar/training_data/smol-smoltalk"
select_column = "source"
select_values = ["smol-magpie-ultra-short", "longalign", "smol-summarize-5k"]
trim_turns = true
epochs = 1
weight = 45
